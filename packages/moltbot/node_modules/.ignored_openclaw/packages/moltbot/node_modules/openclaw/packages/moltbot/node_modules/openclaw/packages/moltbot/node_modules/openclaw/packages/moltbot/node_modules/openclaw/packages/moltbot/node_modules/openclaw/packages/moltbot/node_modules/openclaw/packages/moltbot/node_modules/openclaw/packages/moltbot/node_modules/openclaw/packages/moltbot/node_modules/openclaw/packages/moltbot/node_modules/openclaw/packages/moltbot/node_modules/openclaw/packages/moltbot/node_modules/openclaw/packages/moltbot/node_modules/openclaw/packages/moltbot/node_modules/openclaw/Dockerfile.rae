FROM node:22-bookworm-slim
WORKDIR /app
RUN npm install -g pnpm
COPY . .
RUN pnpm install && pnpm canvas:a2ui:bundle && pnpm build:docker
CMD ["node", "dist/index.js", "gateway", "--bind", "0.0.0.0", "--port", "18789"]
